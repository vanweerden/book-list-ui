import React from 'react';
import { Header } from './Header';
import { BookListContainer } from './BookListContainer';

export const App = () => (
  <div id="app">
    <Header />
    <BookListContainer />
  </div>
);
