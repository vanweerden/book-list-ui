import React from 'react';
import { Header } from './Header';
import { BookTableContainer } from './BookTableContainer';

export const App = () => (
  <div id="app">
    <Header />
    <BookTableContainer />
  </div>
);
